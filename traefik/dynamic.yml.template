http:
  routers:
    product-router:
      rule: "PathPrefix(`/api/product`)"
      entryPoints: ["${ENTRY_POINT}"]
      service: product-service

  services:
    product-service:
      loadBalancer:
        servers:
          - url: "${PRODUCT_SERVICE_URL}"
